{% extends 'base.html.twig' %}

{% block title %} / Liste locations{% endblock %}

{% block body %}
<div class="container d-flex flex-wrap">
    <h1 class="mt-5 mb-3">{{titre}}</h1>
    <div class="row">
        {% for ad in ads %}
            {% include "/ad/_list_ad.html.twig" %}
        {% endfor %}
    </div>
</div>
{% endblock %}